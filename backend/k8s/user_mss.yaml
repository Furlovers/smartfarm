apiVersion: v1
kind: ConfigMap
metadata:
  name: user-mss-config
data:
  JWT_SECRET: "rzz99wLqZDLmrdS&1JHRhyV&crj@DB"
  MONGODB_URI: "mongodb+srv://baguncadomonstrao:%23CabecaDoVandeco%40321@cluster-smartfarm.xailfpc.mongodb.net/smartfarm?retryWrites=true&w=majority&appName=Cluster-SmartFarm"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-mss
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-mss
  template:
    metadata:
      labels:
        app: user-mss
    spec:
      containers:
        - name: user-mss
          image: user_mss:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            limits:
              memory: 256Mi
              cpu: 1
            requests:
              memory: 128Mi
              cpu: 250m
          envFrom:
            - configMapRef:
                name: user-mss-config

---
apiVersion: v1
kind: Service
metadata:
  name: user-mss
spec:
  type: NodePort
  selector:
    app: user-mss
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 31000
