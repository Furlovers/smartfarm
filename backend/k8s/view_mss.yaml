apiVersion: v1
kind: ConfigMap
metadata:
  name: view-mss-config
data:
  JWT_SECRET: "rzz99wLqZDLmrdS&1JHRhyV&crj@DB"
  MONGODB_URI: "mongodb+srv://baguncadomonstrao:%23CabecaDoVandeco%40321@cluster-smartfarm.xailfpc.mongodb.net/smartfarm?retryWrites=true&w=majority&appName=Cluster-SmartFarm"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: view-mss
spec:
  replicas: 1
  selector:
    matchLabels:
      app: view-mss
  template:
    metadata:
      labels:
        app: view-mss
    spec:
      containers:
        - name: view-mss
          image: view_mss:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3003
          resources:
            limits:
              memory: 256Mi
              cpu: 1
            requests:
              memory: 128Mi
              cpu: 250m
          envFrom:
            - configMapRef:
                name: view-mss-config

---
apiVersion: v1
kind: Service
metadata:
  name: view-mss
spec:
  type: NodePort
  selector:
    app: view-mss
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
      nodePort: 31003
